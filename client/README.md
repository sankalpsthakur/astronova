Client (iOS)

- Xcode project: `client/astronova.xcodeproj`
- App sources: `client/AstronovaApp/`

Open the Xcode project and run on iOS 17+. The networking layer points to the backend base URL configured in `client/AstronovaApp/NetworkClient.swift`.
